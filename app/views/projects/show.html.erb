<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @project.title %>
</br>
<b>Technologies used:</b>
<%= @project.technologies_used %>
</p>
<% if @project.image %>
  <td><%= image_tag image-url(@project.image) %>
<% end %>

<%= render 'comments/index' %>

<%= link_to 'Edit', edit_project_path(@project) %>
<%= link_to 'Back', projects_path %>
